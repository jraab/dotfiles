# ~/.inputrc - Readline configuration
# Managed by chezmoi

# Case-insensitive tab completion
set completion-ignore-case on

# Show all matches if ambiguous on first tab
set show-all-if-ambiguous on

# Color files by type during completion (like ls)
set colored-stats on

# Append slash to completed directory names
set mark-directories on
set mark-symlinked-directories on

# Show common prefix before cycling through matches
set menu-complete-display-prefix on

# Don't ring bell on completion
set bell-style none

# Up/down arrow searches history filtered by current input
"\e[A": history-search-backward
"\e[B": history-search-forward
